Runnning:

Install ionic CLI:
npm install -g ionic

git clone ...
cd
npm install
bower install
ionic state restore
cordova prepare
open platforms/ios/YokaiGuide.xcodeproj

Development:

gulp watch
ionic serve --lab
